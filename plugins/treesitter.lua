return {
  "nvim-treesitter/nvim-treesitter",
  opts={
    -- Highly recommended to install treesitter-cli on your machine to use auto-install
    auto_install=true,
    autotag={
      enable=true
    }
  }
}
